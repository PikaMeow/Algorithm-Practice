//输入一个正整数n,然后读取n个正整数a1,a2...，最后再读取一个正整数m,统计有多少个正整数小于m
#include <stdio.h> 
int main () {
 FILE *fin,*fout;
 fin = fopen("stat.in","rb");
 fout = fopen("stat.out","r+");
 int n ,x, m;
 fscanf(fin,"%d",&n);
 
 int n,
 return 0;
}
